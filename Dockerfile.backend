# Use OpenJDK since your backend is Spring Boot (Java)
FROM openjdk:17-jdk-slim

# Install git
RUN apt-get update && apt-get install -y git maven && rm -rf /var/lib/apt/lists/*

# Clone your backend repo
RUN git clone https://github.com/seshasaikrishnachanumolu/cicd-project-backend.git /app

WORKDIR /app
<<<<<<< HEAD
# Clone your backend repository directly into the image
RUN git clone https://github.com/seshasaikrishnachanumolu/cicd-project-backend.git .
RUN mvn clean package -DskipTests

FROM eclipse-temurin:21-jre-alpine
WORKDIR /app
COPY --from=build /app/target/SDPProject-0.0.1-SNAPSHOT.jar app.jar
EXPOSE 8086
ENTRYPOINT ["java", "-jar", "app.jar"]
=======

# Build Spring Boot project
RUN mvn clean package -DskipTests

# Expose backend port (matches your compose file -> 8086)
EXPOSE 8086

# Run the JAR
CMD ["java", "-jar", "target/*.jar"]
>>>>>>> a1d975e (first commit)
